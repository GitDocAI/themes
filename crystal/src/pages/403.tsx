const Forbidden = () => {
  return (
    <div style={{
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      height: '100vh',
      fontSize: '2rem',
      fontWeight: 'bold',
    }}>
      You are not allowed to see this page.
    </div>
  );
};

export default Forbidden;
